# Copy to .env to override the host ports docker-compose publishes.
# Nothing here is a secret; the app takes no API keys, tokens or database
# credentials (see CLAUDE.md — simulated only).

HTTP_PORT=80
HTTPS_PORT=443
